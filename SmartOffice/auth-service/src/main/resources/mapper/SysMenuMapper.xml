<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqf.auth.mapper.SysMenuMapper">

    <select id="getMenuByRoleIds" resultType="com.cqf.auth.model.po.SysMenu">
        SELECT DISTINCT
        m.id,
        m.parent_id,
        m.name,
        m.path,
        m.component,
        m.menu_type,
        m.icon,
        m.sort,
        m.visible,
        m.permission,
        m.create_time
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id IN
        <foreach collection="list" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        GROUP BY m.id
    </select>
</mapper>
