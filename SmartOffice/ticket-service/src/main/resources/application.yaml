server:
  port: 8083

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
so:
  db:
    database: so-ticket

logging:
  level:
    com.cqf.knowledge.mapper: debug
