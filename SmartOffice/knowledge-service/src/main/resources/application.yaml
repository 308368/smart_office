spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
server:
  port: 8082

so:
  db:
    database: so-knowledge

logging:
  level:
    com.cqf.knowledge.mapper: debug
